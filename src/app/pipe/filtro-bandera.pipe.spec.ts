import { FiltroBanderaPipe } from './filtro-bandera.pipe';

describe('FiltroBanderaPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroBanderaPipe();
    expect(pipe).toBeTruthy();
  });
});
