import { FiltrocontinentePipe } from './filtrocontinente.pipe';

describe('FiltrocontinentePipe', () => {
  it('create an instance', () => {
    const pipe = new FiltrocontinentePipe();
    expect(pipe).toBeTruthy();
  });
});
